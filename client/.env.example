# In dev this isn't needed — Vite proxies /api to the Express server (see vite.config.js).
# Set this only for a production build pointing at a deployed API:
VITE_API_URL=https://your-deployed-api.example.com/api
